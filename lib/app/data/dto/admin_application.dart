import 'package:stamp_now/app/data/model/application.dart';

class AdminApplication {
  final Application application;
  final String profileImage;

  AdminApplication(
      {required this.application, required this.profileImage});
}
