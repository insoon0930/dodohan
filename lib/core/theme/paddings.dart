import 'package:flutter/material.dart';

class ThemePaddings {
  static const double mainPadding = 16.0;
  static const double switchPadding = 3.0;
  // static const EdgeInsets mainPaddingAll = EdgeInsets.all(mainPadding);
  // static const EdgeInsets mainPaddingHorizontal = EdgeInsets.symmetric(horizontal: mainPadding);
  // static const EdgeInsets mainPaddingVertical = EdgeInsets.symmetric(vertical: mainPadding);

  // static const double statusBarHeight = 16.0;
}